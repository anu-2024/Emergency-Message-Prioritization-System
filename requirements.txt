# CPU-only PyTorch wheels (much smaller download than the default CUDA
# build) — this project never requires a GPU (SRS §2.4).
--extra-index-url https://download.pytorch.org/whl/cpu

# Core
streamlit>=1.36,<2.0
pandas>=2.0,<3.0
numpy>=1.26,<2.1
plotly>=5.20,<6.0

# NLP
scikit-learn>=1.4,<1.6
joblib>=1.3,<2.0
spacy>=3.7,<3.8
# Pinned spaCy English model wheel — installs directly via pip (no separate
# `python -m spacy download` step), so a fresh `pip install -r requirements.txt`
# on Streamlit Community Cloud is enough to get statistical NER working.
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl

# RL
gymnasium>=0.29,<1.1
stable-baselines3>=2.2,<3.0
torch>=2.2,<2.4

# Notebook (only needed to *run/edit* nlp_training.ipynb; harmless in the
# deployed app if present, and easy to remove for a slimmer web deploy)
notebook>=7.0
matplotlib>=3.8,<4.0
